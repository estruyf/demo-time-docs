---
title: Time actions
description: Perform time actions in your demos like waiting for a specific time, waiting for user input, and more.
lastmod: 2025-02-16T16:18:55.297Z
---

import { Aside } from '@astrojs/starlight/components';
import GitHubFileOpener from '../../../components/GitHubFileOpener.astro';

Time actions are used to perform actions related to time in the editor. You can use these actions to wait for a specific time, wait for user input, and more.

<Aside type="note" title='Samples'>
  You can find time actions samples on GitHub: 
  <GitHubFileOpener filePath=".demo/time-actions.json" />
</Aside>

## Wait for a specific time

Wait for a specific time before executing the next action.

```json
{
  "action": "waitForTimeout",
  "timeout": "<timeout in milliseconds>"
}
```

## Wait for user input

Wait until the user presses a key.

```json
{
  "action": "waitForInput"
}
```